export const metadata = {
  title: 'Profile Settings | Valoria Institute',
  robots: { index: false, follow: false },
}

export default function ProfileSettingsLayout({ children }) {
  return children
}
